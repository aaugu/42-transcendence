import {inputValue} from "./utils.js"

export function signUpButton() {
    const userName = inputValue('username');
    const nickName = inputValue('nickname');
    const email = inputValue('email');

    //add functionality to delete when sign up is clicked
    //add redirection when signup is clicked
    //add variables to localstorage + create variable userLoggedIn == true
}