export async function startRemoteTournament() {
    //generateMatchView returns match list
    //startTournamentView (patch), give tourn_id
    //startMatchView (post), give tourn_id, body: player1, player2

    startGame();
}